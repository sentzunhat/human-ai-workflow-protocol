# final audit of core HAWP kit and provider install instructions

**Backlog ID (Legacy):** — (UUID-native item)
**UUID:** `3030ebff-55b0-4d34-ad76-0909c0fa552c`
**Type:** improvement
**Reported:** 2026-07-08
**Risk Level:** medium

---

### Input (what was reported)

> amazing and thank you so much lets start by doing a last audit of the core hawp folder and then a review of the providers instructions to install the kit and then we will be golden then line up the compoundable work items please

---

### Context

This was a final readiness pass before continuing the remaining compoundable
librarian work. The focus was the distributable HAWP source under `core/.hawp/`
and provider install/update instruction coverage.

---

### Analysis

**Root cause (or most likely cause):**
_The source repo's local `.hawp/kit` had moved ahead of the distributable
`core/.hawp/kit`, and Claude provider generated guides existed without being
registered consistently in indexes, manifest, materialization targets, and
distribution composition._

**Directly verified:**
_Initial `diff -qr .hawp/kit core/.hawp/kit` showed missing workflow-loop docs
and multiple drifted kit files in `core/.hawp/kit`. `find core/.hawp -name
'.DS_Store'` found `core/.hawp/.DS_Store`. `distribution/generated/claude/**`
existed, while provider indexes and composition omitted Claude._

**Inferred (not yet proven):**
_Downstream installs from `core/.hawp` would have missed current workflow-loop
guidance until the kit source was resynced._

**Scope — what else is affected:**
_`core/.hawp/kit/**`, provider indexes/manifests, provider materialization,
distribution composition, generated install/update guides, and test labels._

---

### Outcome

Synced the distributable `core/.hawp/kit` from the repo-local `.hawp/kit`,
removed the stray `core/.hawp/.DS_Store`, registered Claude Code as a first-class
active provider in provider docs and `core/providers/manifest.yaml`, added Claude
rules to provider materialization validation, included Claude in distribution
composition/test coverage, and regenerated all provider install/update guides.

Also updated shared install/update guide fragments so the provider examples name
Claude Code and Codex alongside GitHub, Cursor, and Continue.

Follow-up documentation drift check updated root, librarian, provider, and shared
provider docs so future provider additions have a clear source-pack, manifest,
distribution-fragment, active-provider, materialization, index, and sync checklist.

---

### Verification

- [x] Core kit matches local kit: Evidence: `diff -qr .hawp/kit core/.hawp/kit` produced no output after sync.
- [x] No `.DS_Store` remains in the distributable core: Evidence: `find core/.hawp -name '.DS_Store' -print` produced no output after cleanup.
- [x] Core kit validates: Evidence: `npm --prefix librarian run kit:validate -- --kit-path <repo-root-abs>/human-ai-workflow-protocol/core/.hawp/kit` passed 3 checks with 0 issues.
- [x] Full librarian validation passes: Evidence: `npm --prefix librarian run validate` passed typecheck, 54/54 tests, markdown link check across 319 Markdown files, `kit:validate`, `distribution:sync`, and `work:validate`.
- [x] Provider generated guides are current: Evidence: `distribution:sync` reported `distribution validation passed: generated outputs are current` after regenerating 20 active provider guide variants.
- [x] Provider onboarding docs are current: Evidence: `rg -n 'twelve|three providers|GitHub/Cursor/Continue|Outputs twelve|materializes GitHub/Cursor/Continue|npm run distribution:sync' README.md librarian/README.md core/providers core/providers/shared distribution/sources/providers distribution/generated/README.md` only returned the valid `librarian/README.md` command used after `cd librarian`.
- [x] Markdown links validate after the documentation drift pass: Evidence: `npm --prefix librarian run check:markdown-links` checked 320 Markdown files with valid local links.
- [x] Work records validate: Evidence: `work:validate` reported `VALIDATION PASS` with 0 issues and 1 existing verification-clarity warning.

## Close Checklist

- [x] Outcome section filled
- [x] Verification section filled
- [x] Plan file saved under `closed/2026/07/08/3030ebff-55b0-4d34-ad76-0909c0fa552c.md`
- [x] BACKLOG.md updated
