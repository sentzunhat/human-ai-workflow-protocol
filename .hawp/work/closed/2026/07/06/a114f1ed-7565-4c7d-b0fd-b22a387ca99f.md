# add Codex provider pack before broader librarian CLI work

**Backlog ID (Legacy):** — (UUID-native item)
**UUID:** `a114f1ed-7565-4c7d-b0fd-b22a387ca99f`
**Type:** improvement
**Reported:** 2026-07-06
**Risk Level:** medium

---

### Input (what was reported)

> Let's go with 6 first the cli can come after

---

### Context

The ranked list placed the deferred TASK-079 Phase 7 Codex lane after workflow-loop
and librarian CLI foundations. The user explicitly chose the Codex lane first and
deferred the broader CLI work.

Within the current provider system, the smallest Codex-first slice is a Codex
provider install pack for repo-local `AGENTS.md` instructions. This is distinct
from a future `codex-cli` participant adapter for runtime workflow-loop
orchestration.

---

### Analysis

**Root cause (or most likely cause):**
_HAWP already has provider packs for GitHub, Cursor, Continue, and a Claude source
pack, but no Codex-specific provider pack. Codex can use `AGENTS.md`, so the
provider work can start without waiting for an installable librarian CLI._

**Directly verified:**
_`core/providers/README.md` listed GitHub, Cursor, and Continue install mappings,
but not Codex. `core/providers/manifest.yaml` had no Codex provider entry.
`distribution/generated/` had no `codex/` generated guides. TASK-079 listed
`codex-cli` as a deferred participant adapter, not as an install provider pack._

**Inferred (not yet proven):**
_Codex provider installation should be `AGENTS.md` only for this slice; no
`.codex/` folder or runtime adapter should be invented._

**Scope — what else is affected:**
_`core/providers/`, `distribution/sources/providers/`, generated distribution
guides, and distribution composition. Future runtime adapter work remains
separate._

---

### Work Coordination

**Owner:** agent
**Implementation status:** in-progress
**Overlapping files:**

- `.hawp/work/BACKLOG.md`
- `core/providers/README.md`
- `core/providers/manifest.yaml`
- `core/providers/.codex/**`
- `distribution/sources/providers/codex/**`
- `distribution/generated/codex/**`
- `librarian/scripts/librarian/distribution/shared/composition.ts`

**Parallel work risk:** medium
**Can implement now:** yes — explicit user approval pulled this lane ahead of the broader CLI sequence.

**Coordination note:**
_This item is provider-pack work only. It does not implement the future `codex-cli`
participant adapter or installable librarian CLI._

**Repo-root proof (required before path-sensitive edits):**

```text
pwd
<repo-root-abs>/human-ai-workflow-protocol

git rev-parse --show-toplevel
<repo-root-abs>/human-ai-workflow-protocol

git rev-parse --show-prefix

git status --short
```

---

### Options

#### Option A — Codex provider pack first

Add `core/providers/.codex/` with `AGENTS.md.seed`, distribution source
fragments, manifest/docs, and generated install/update guides.

Trade-offs: useful immediately for Codex users; keeps runtime orchestration out of
scope.

#### Option B — Codex CLI participant adapter first

Start implementing the TASK-079 `codex-cli` runtime adapter before provider-pack
support.

Trade-offs: closer to the original Phase 7 language, but depends on workflow-loop
runtime pieces the repo has intentionally deferred.

---

### Recommended Fix

**Option chosen:** A
**Rationale:**

The user asked to pull Codex work ahead of the CLI. A Codex `AGENTS.md` provider
pack gives immediate value while preserving HAWP's boundary that the workflow
loop remains instruction/artifact-based until runtime orchestration is explicitly
implemented.

**Files to change:**

- `.hawp/work/BACKLOG.md` — add active Codex provider row
- `core/providers/.codex/AGENTS.md.seed` — Codex repo instruction seed
- `core/providers/.codex/README.md` — provider pack docs
- `core/providers/README.md` — add Codex install mapping
- `core/providers/manifest.yaml` — add Codex provider mapping
- `distribution/sources/providers/codex/**` — install/update source fragments
- `librarian/scripts/librarian/distribution/shared/composition.ts` — include Codex in generated guides
- `distribution/generated/codex/**` — generated install/update guides

**What to verify after:**

- [ ] `npm --prefix librarian run providers:validate`
- [ ] `npm --prefix librarian run distribution:sync`
- [ ] `npm --prefix librarian run work:validate`
- [ ] Generated Codex guides exist for install/update on main/dev

---

## Outcome (filled at close)

Implemented the Codex provider-pack slice ahead of the broader librarian CLI lane.

Added `core/providers/.codex/` with an `AGENTS.md.seed` and provider README, registered
Codex in `core/providers/manifest.yaml` and `core/providers/README.md`, added Codex
distribution source fragments under `distribution/sources/providers/codex/`, included
Codex in distribution composition, updated distribution indexes, and generated
install/update guides under `distribution/generated/codex/`.

Kept runtime orchestration out of scope: this item does not create a `.codex/`
folder, a `codex-cli` participant adapter, or an installable librarian CLI.

## Verification (filled at close)

- [x] Codex generated guides exist: Evidence: `npm --prefix librarian run distribution:sync` created `distribution/generated/codex/install/main.md`, `distribution/generated/codex/install/dev.md`, `distribution/generated/codex/update/main.md`, and `distribution/generated/codex/update/dev.md`.
- [x] Provider and distribution outputs are current: Evidence: `npm --prefix librarian run validate` completed `distribution:sync` with `distribution validation passed: generated outputs are current`.
- [x] Full librarian quality suite passes: Evidence: `npm --prefix librarian run validate` passed typecheck, 54/54 tests, markdown link check across 318 Markdown files, `kit:validate`, `distribution:sync`, and `work:validate`.
- [x] HAWP work records validate: Evidence: `work:validate` reported `VALIDATION PASS` with 0 issues and 1 existing verification-clarity warning.

## Close Checklist

- [x] Outcome section filled
- [x] Verification section filled
- [x] Plan file moved to `closed/2026/07/06/a114f1ed-7565-4c7d-b0fd-b22a387ca99f.md`
- [x] BACKLOG.md updated
