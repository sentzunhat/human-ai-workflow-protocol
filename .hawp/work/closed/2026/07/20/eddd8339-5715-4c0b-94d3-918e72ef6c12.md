# port Phase 2 mutating librarian commands to Go with guardrails

**Backlog ID (Legacy):** — (UUID-native item)
**UUID:** `eddd8339-5715-4c0b-94d3-918e72ef6c12`
**Type:** feature
**Reported:** 2026-07-20
**Risk Level:** medium

---

### Input (what was reported)

> Continue until mutation works in the Go CLI: `kit normalize` and
> `work normalize` with their guardrails.

---

### Context

Executes Phase 2 of
`.hawp/work/notes/2026/07/20/librarian-ts-to-go-port-plan.md`. Depends on
Phase 1 (`39bc92b6-5e3f-49ae-970d-6126ec2cfd82`) for the shared
infrastructure and the validate passes that normalize composes with.

---

### Analysis

**Root cause (or most likely cause):**
_Mutating commands are the highest-risk part of the port; they must keep
the fail-closed clean-worktree guard and dry-run/apply semantics exactly._

**Directly verified:**
_TS sources: `kit-normalize` (24K, mutations/), `work-normalize` (144K,
detection/rules with a 516-line evaluator, models/, output/)._

**Inferred (not yet proven):**
_Rule-by-rule table-driven porting keeps behavior reviewable; dry-run
output diffs against the TS CLI provide parity evidence._

**Scope — what else is affected:**
_`librarian/go/` kit/work packages, clean-tree guard in
`internal/infrastructure/repo`, `backlog upgrade` alias routing._

---

### Recommended Fix

- `hawp kit normalize [--apply]` — kebab-case renames + internal link
  rewrites; dry-run by default.
- `hawp work normalize [--dry-run --apply --validate]` + `backlog upgrade`
  alias — port the detection rules table-driven, one Go test per rule.
- Clean-worktree guard: mutating runs fail closed on a dirty tree.

**What to verify after:**

- [x] Dry-run output parity with TS on this repo (finding counts)
      (Evidence: side-by-side run 2026-07-20 — both report 2 blocked B7
      operations on the same two closed records with identical candidate
      claims; kit normalize both report "No kit normalization needed.")
- [x] Mutating run refused on a dirty worktree
      (Evidence: `hawp backlog upgrade --apply` on the dirty session tree
      exited 2 with the clean-tree error)
- [x] `--apply` on a fixture repo produces the expected file state
      (Evidence: TestApplyClosedRecordNormalization — scaffolds Backlog ID +
      Outcome/Verification/Close Checklist, reconciles date-prefixed paths,
      idempotent on re-run)
- [x] Every ported rule has passing + failing test cases
      (Evidence: normalize_test.go covers A1-A6, A8, B1-B3, B5, B7, the B4
      Evidence-line fix, and the follow-up queue; kit normalize_test.go
      covers renames, link rewrites, and overwrite refusal)

---

## Outcome (filled at close)

Closed 2026-07-20. `hawp kit normalize [--apply]` and
`hawp work normalize` (full flag set: --dry-run/--apply, --validate,
--format text|json, --output, --export-plan, --export-research-queue,
--force-dirty, --verbose) plus the `backlog upgrade` alias are implemented
in `librarian/go/`: detection backlog parser, plan scanner, rule evaluator
(A1-A8, B1-B5, B7), detection report with sync plan and research queue,
and apply-mode closed-record normalization with the fail-closed
clean-worktree guard.

Divergence (bug fix, documented in code): the TS B4 rule was dead code —
its section regex used `\Z`, which JavaScript treats as a literal
case-insensitive "z", truncating the section body. The Go B4 works and
additionally skips Evidence-backed lines that merely quote the word
"unproven", aligning with verification-clarity semantics.

## Verification (filled at close)

- Evidence: `make check` in `librarian/go/` — vet, all 9 test files, build.
- Evidence: dry-run parity run 2026-07-20 (2 B7 operations, matching items
  and claims; identical kit normalize output).
- Evidence: dirty-tree apply refusal exited 2 on the live repo.

## Close Checklist

- [x] Outcome section filled
- [x] Verification section filled
- [x] Plan file saved under `closed/2026/07/20/eddd8339-5715-4c0b-94d3-918e72ef6c12.md`
- [x] BACKLOG.md updated
