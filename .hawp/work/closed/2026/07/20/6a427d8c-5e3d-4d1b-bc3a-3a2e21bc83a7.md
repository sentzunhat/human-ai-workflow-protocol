# consolidate librarian workspaces — move Go scaffold into librarian/, remove golang/ and nodejs/

**Backlog ID (Legacy):** — (UUID-native item)
**UUID:** `6a427d8c-5e3d-4d1b-bc3a-3a2e21bc83a7`
**Type:** improvement
**Reported:** 2026-07-20
**Risk Level:** low

---

### Input (what was reported)

> Remove and clean up the two `golang/` and `nodejs/` project folders, and
> start with the scaffolding or cleanup of the librarian. Decision: the Go
> scaffold moves to a `librarian/go/` subfolder so `librarian/` becomes the
> single librarian home; npm scripts stay alongside until ported to Go.

---

### Context

Follows the Go-first decision (`56067dc8-1cc6-4dbe-8b6d-dd7ffdfea80c`).
`nodejs/` is a README-only direction marker; `golang/` holds the Zacatl-shaped
Go scaffold (cmd/hawp + internal layers, ~1.6 MB binary proof). The working
npm tooling lives in `librarian/`.

---

### Analysis

**Root cause (or most likely cause):**
_Three top-level folders (`librarian/`, `golang/`, `nodejs/`) describe one
product; the split was a transition marker that is no longer needed once the
Go lane is decided._

**Directly verified:**
_Only `README.md` (repo root) references the `golang/` and `nodejs/` folders
outside the folders themselves; `.hawp/kit/start-here.md` does not._

**Inferred (not yet proven):**
_Moving the Go module preserves buildability since the module is
self-contained (no external references into the old path)._

**Scope — what else is affected:**
_Root `README.md` layout table, `librarian/go/README.md` build instructions,
plan `56067dc8` context wording (references `golang/` workspace)._

---

### Recommended Fix

- `git mv golang librarian/go`; delete `nodejs/`.
- Update `librarian/go/README.md` build path and framing (subfolder of the
  librarian home, not a parallel lane).
- Update root `README.md` layout entries.
- Verify `go build` from the new location and run repo validations.

**What to verify after:**

- [ ] `go build ./cmd/hawp` succeeds from `librarian/go/`
- [ ] No remaining references to top-level `golang/` or `nodejs/`
- [ ] `work:validate` and markdown link check pass

---

## Outcome (filled at close)

Closed 2026-07-20. The Go scaffold moved via `git mv golang librarian/go`
(history preserved); `nodejs/` (README-only marker) was removed. Root
`README.md` layout section and `librarian/go/README.md` were rewritten for
the new location, `.gitignore` updated (`golang/bin/` → `librarian/go/bin/`),
and path references in the new active plan files were updated.

## Verification (filled at close)

- Evidence: `go build -trimpath -ldflags='-s -w' -o bin/hawp ./cmd/hawp`
  succeeded from `librarian/go/` and `./bin/hawp --help` printed the CLI
  usage (run 2026-07-20).
- Evidence: `git check-ignore librarian/go/bin/hawp` confirms the binary
  output stays untracked.
- Evidence: `npm --prefix librarian run check:markdown-links` — 329 files
  checked, all local links valid.
- Evidence: `npm --prefix librarian run work:validate` — no issues in this
  item; the single reported failure is the pre-existing missing Close
  Checklist in `closed/2026/07/11/472d447b-…` (tracked separately).
- Evidence: `grep` shows no remaining references to top-level `golang/` or
  `nodejs/` folders outside historical plan/evidence records and the
  unrelated `standards/nodejs/` category.

## Close Checklist

- [x] Outcome section filled
- [x] Verification section filled
- [x] Plan file saved under `closed/2026/07/20/6a427d8c-5e3d-4d1b-bc3a-3a2e21bc83a7.md`
- [x] BACKLOG.md updated
