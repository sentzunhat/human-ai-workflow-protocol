# hmcp0825 — Codex-side HAWP MCP tool visibility investigation

**Type:** investigation  
**Status:** done  
**Opened:** 2026-08-25
**Closed:** 2026-08-25

## Goal

Determine whether missing `hawp_*` tools in fresh Codex sessions are caused by repo setup, Codex desktop/session loading, deferred tool indexing, or caching behavior.

## Constraints

- Investigate Codex desktop/session/tool-loading behavior only
- Do not continue into printer work
- Do not change repo HAWP files unless host-side evidence points back to them
- Prefer direct host/session evidence over inference

## Outcome

The repo-scoped HAWP MCP configuration in `local-print-farm` is present and recognized by the host Codex CLI. Earlier Codex local logs showed repeated omission at session-tool-catalog resolution: `omitting MCP server without an exact ready client server_name=hawp`. After the absolute-path Codex MCP config fix, a fresh host-side Codex task rooted at `local-print-farm` sees the expected deferred tools again (`hawp_search: 1`, `hawp_work_new: 1`, `hawp_work_validate: 1`, `hawp: 3`). Within inspected scope, no extra MCP server/client layer is needed beyond the existing `hawp mcp` stdio entry.

## Verification

- [x] Confirmed target checkout root and inspected `.codex/config.toml` in `local-print-farm`. Evidence: this plan's What was done section names the checked config path.
- [x] Ran `codex mcp list` in `local-print-farm`. Evidence: this plan's What was done section records the host-side Codex CLI check.
- [x] Ran `codex mcp get hawp` in `local-print-farm`. Evidence: this plan's What was done section records the host-side Codex CLI check.
- [x] Probed deferred tool inventory for `hawp_search`, `hawp_work_new`, `hawp_work_validate`, and `hawp`. Evidence: this plan's Outcome section records the deferred tool counts.
- [x] Compared against another live deferred MCP server (`node_repl`). Evidence: this plan's What was done section records the `node_repl` comparison.
- [x] Inspected Codex desktop logs and `~/.codex/logs_2.sqlite`. Evidence: this plan's What was done section records the desktop log inspection.
- [x] Verified a fresh host-side Codex task in `local-print-farm` now sees `hawp*`. Evidence: this plan's Outcome section records the fresh-task tool counts.

## What was done

- Verified `hawp` repo config is present and enabled in `local-print-farm/.codex/config.toml`
- Verified host-side Codex CLI sees `hawp` and the expected enabled tools
- Verified live deferred discovery still returns zero tools for `hawp*`
- Verified deferred discovery does return tools for `node_repl`
- Isolated the failure point in Codex logs to `omitting MCP server without an exact ready client server_name=hawp`
- Verified a fresh host-side `codex exec` task now reports `hawp_search: 1`, `hawp_work_new: 1`, `hawp_work_validate: 1`, `hawp: 3`, `node_repl: 3`

## Evidence

- See status companion: `.hawp/work/status/2026/08/25/hawp-mcp-codex-tool-visibility-checkpoint.md`

## Close Checklist

- [x] Outcome recorded
- [x] Verification filled with directly observed scope
- [x] Evidence/status companion linked
- [x] Ready to stay in closed history
