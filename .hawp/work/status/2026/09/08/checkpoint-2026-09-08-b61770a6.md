# Status Checkpoint — 2026-09-08 — b61770a6 adapter migration

**Branch:** `feature/v0.0.24-work-folder-normalization`
**Last commit:** `8cfda10` feat(b61770a6 slice 1): move Ollama and None adapters to infrastructure/models

## What is complete

### Slice 1 — Ollama + None adapters (commit `8cfda10`)

- `infrastructure/models/ollama/` — `OllamaEmbedder` and `OllamaLLMClient` with tests; moved from `domain/embeddings/` and `domain/llm/`
- `infrastructure/models/none/` — `NullEmbedder` and `NullLLMClient`; moved from domain
- `infrastructure/models/factory.go` — package `models`; replaces domain factories; routes "ollama"/"none"/"onnx" to correct adapters
- `domain/embeddings/embedder.go` — interface + `EmbeddingResult` only (no factories)
- `domain/llm/llm_client.go` — `LLMClient` interface + `ReshapingPrompt` const + ONNX model catalog (`ModelInfo`, `SupportedModels`, `DefaultModel`) — catalog items are temporary until slice 2
- All application callers updated to `inframodels.NewEmbedder` / `inframodels.NewLLMClient`
- ONNX remains in domain as temp placeholder (`embeddings.NewONNXEmbedder`, `llm.NewONNXLLMClient`)

### Housekeeping (commit `462fa83`)

- `e7a5e294` CLI decomposition plan closed to `closed/2026/09/08/e7a5e294/`
- CHANGELOG.md and BACKLOG.md updated
- STATUS.md, package-boundaries.md, and 47c793d6 plan links corrected

## What is next

### Slice 2 — ONNX adapters (in progress this turn)

Move `domain/embeddings/onnx_embedder.go` and `domain/llm/onnx_client.go` to `infrastructure/models/onnx/`.

File plan:
- CREATE `infrastructure/models/onnx/embedder.go` — package `onnx`
- CREATE `infrastructure/models/onnx/llm.go` — package `onnx`
- CREATE `infrastructure/models/onnx/embedder_test.go` — package `onnx`
- CREATE `infrastructure/models/onnx/llm_test.go` — package `onnx`
- ADD `const DefaultModel` to `domain/embeddings/embedder.go` (embed-service uses `embeddings.DefaultModel`)
- REMOVE `ModelInfo`, `SupportedModels`, `DefaultModel` from `domain/llm/llm_client.go`
- DELETE `domain/embeddings/onnx_embedder.go` and `onnx_embedder_test.go`
- DELETE `domain/llm/onnx_client.go` and `onnx_client_test.go`
- UPDATE factory: import `onnxmodel` and call `onnxmodel.NewONNXEmbedder` / `onnxmodel.NewONNXLLMClient`
- UPDATE `domain/integration_test.go` and `domain/benchmark_test.go`: import onnx package for ONNX constructors and catalog

Symbol renaming within `package onnx` (to avoid conflict between embedder and LLM):
- `ModelInfo` → `EmbedModelInfo` (embedder), `LLMModelInfo` (LLM)
- `SupportedModels` → `SupportedEmbedModels` (embedder), `SupportedLLMModels` (LLM)
- `DefaultModel` → `DefaultEmbedModel` (embedder; `embeddings.DefaultModel` stays in domain as alias)
- `DefaultModel` LLM → `DefaultLLMModel`
- `GetModelPath` → `GetEmbedModelPath`, `GetLLMModelPath`
- `pipelineCounter` — shared single counter at package level

## Constraints

- `core/.hawp/bin/hawp` binary deletion is NOT staged. Must not be committed until after PR merges to main.
- No PR or merge in this session.

## Remaining after slice 2

- `b61770a6` step 3: create `internal/bootstrap/` for explicit service wiring
- `b61770a6` step 4: move usage SQL implementation to `infrastructure/repositories/usage/`
- `742aa60b`: separate work domain rules, use cases, and filesystem adapters
- Open PR: `feature/v0.0.24-work-folder-normalization` → `development` → `main`
